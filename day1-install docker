#from this documentation complete step 1 and step 2
  #once completed with the step 1 and step2
  https://docs.docker.com/engine/install/ubuntu/
  #to check docker version
   26  docker --version'
  #this will give you information about docker
   27  docker info
  ##all the command you can see using this 
   28  docker
#lets create our first container
#this will create the fdirst container
 34  docker container run --publish 80:80 --detach nginx
#this will show the container
   35  docker ps
##curl localhost will show the test page of the container
   36  curl localhost
######check the ip address of the docker machine 
ip a
###copy the ip and paste it in the browser you will see the test page of nginx

#next time when we create a container with same image it will not download as the image is availble in local repo
   59  docker container run --publish 81:80 --detach nginx
   60  docker images
#lets create a new container with new image it will download
   61  docker container run --publish 82:80 --detach httpd
   62  docker ps
  #if you look into all the container the name is dictionary generate if you want to give a custom name --name
   64  docker run -p 83:80 -d --name webhost nginx
   65  docker ps
#lets find our where docker keep the logs files and hostname path in your local system
 68  docker ps
#we will check the webhost containst
   69  curl localhost:83
#if you want to see the logs of your container
   71  docker logs webhost
#below command will show all the information about your container
   72  docker container inspect webhost
##you need to paste to log file path and the ip address of the cotnainer
#container vs process id
#we will create a mongo db container
 76  docker run --name mongo -d mongo
   77  docker ps
 #top command is showing that the container is running as process id
   80  docker top mongo
   81  ps aux
#check the process id of the container
   82  docker top mongo
##replace this with your own process id while killing
   83  kill -9 7185
   84  docker ps
#docker ps -a will show running and stopped container
   85  docker ps -a
#if you want to start a container
   86  docker start mongo
   87  docker ps
#stop a container
   88  docker stop mongo
#this will show all the container utilization report
   92  docker container stats
#press ctr c to exit
##--detach will make your container run as background or deamon process if you dont give --detach it will run the container as foreground process. 
 25  docker run -p 84:80 nginx
   26  docker run -p 84:80 -d nginx
####docker networking

