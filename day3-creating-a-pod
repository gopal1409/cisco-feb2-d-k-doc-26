#lets create our first pod 
   36  kubectl run
  #to create the pod you can use the help
   37  kubectl run --help
  ##while creating the pod we need to define name and image below mentioned will do a dry and automatically generate an yaml file
   38  kubectl run my-first-pod --image=nginx --port=80 --dry-run=client -o yaml
   39  kubectl run my-first-pod --image=nginx --port=80 --dry-run=client -o yaml > pod2.yml
  ###if you want to see the yaml file
   40  cat pod2.yml
  #to apply the pod 
   41  kubectl apply -f pod2.yml
  #get pod will show that there is a pod runnign wait fir 2 min till it starty running
   42  kubectl get pod
  #this will show that where your pod is running
   43  kubectl get pod -o wide
  #describe will provide more information about the pod 
   44  kubectl describe pod my-first-pod
  ###finally putting some more information about the pod 
  #kubectl apply -f pod.yml
#all the yaml file will run from top to bottom and left to right
apiVersion: v1 #this is the version of the api
kind: Pod #it is the name of the api
metadata: #using pod metadata
  name: nginx #name of the pod
spec: #we need to define specification of the pod
  containers: 
  - name: nginx
    image: nginx:1.14.2
    ports:
    - containerPort: 80
  - name: apache
    image: apache
    ports:
    - containerPort: 81
##lets create an error full container #change the image to your first image which is java based 
 52  kubectl run my-custom-image --image=gopal1409/helloapp --port=8080 --dry-run=client -o yaml > pod3.yml
   53  cat pod3.yml
   54  kubectl apply -f pod3.yml
##after a minute or so do get pod you will see some crash loopback error 
   55  kubectl get pod
#using kubectl logs we will see why we are geeting the error
   56  kubectl logs my-custom-image
##lets delete the container 
 64  kubectl delete pod my-custom-image
#now change the image to an python based app which we have upload yeasterday if you see we are running the python app in port 50000
   65  kubectl run my-custom-image --image=gopal1409/my-flask-app --port=5000 --dry-run=client -o yaml > pod3.yml
   66  kubectl apply -f pod3.yml
#check the pod is running after 1 min
   67  kubectl get pod
#check the logs of the python app
   68  kubectl logs my-custom-image
#finally you can see the pod is in running state
   69  kubectl get pod

